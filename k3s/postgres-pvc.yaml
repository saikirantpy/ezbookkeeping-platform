apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: ezbookkeeping

spec:
  storageClassName: local-path

  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi
